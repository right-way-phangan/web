/**
 * Сегментная граница 404.
 *
 * Корневой app/not-found.tsx отрабатывает только на несопоставленном пути
 * (/zzz-nope). Стоит notFound() сработать ВНУТРИ сопоставленного роута —
 * хоть из generateMetadata (объекты), хоть из тела страницы (проекты, статьи,
 * районы) — Next 15 отдаёт свой встроенный документ <html id="__next_error__">
 * вместо нашего дерева: статус 404 верный, тело пустое. Проверено на проде
 * 2026-08-06 на /object/RW-9999, /projects/nope-xyz, /knowledge/nope-xyz,
 * /districts/nope-xyz. Граница в самом сегменте возвращает нормальную страницу.
 */
export { default } from "@/app/not-found";
