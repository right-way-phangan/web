/**
 * Реальные отзывы клиентов и партнёров. Секция на сайте (главная, /about)
 * рендерится ТОЛЬКО когда здесь есть цитаты нужной локали — пустой массив =
 * секции нет. Выдуманные отзывы не добавлять никогда: только настоящие слова
 * настоящих людей с их согласия. Пара EN/RU не обязана быть переводом —
 * подбираем живые цитаты под каждую локаль.
 */
export interface Testimonial {
  quote: string;
  /** Имя или «Имя, город» — как согласовано с автором цитаты. */
  name: string;
  /** Контекст: «купил участок в Шри Тану», «застройщик, 12 вилл» и т.п. */
  role: string;
  lang: "en" | "ru";
}

export const TESTIMONIALS: Testimonial[] = [];
