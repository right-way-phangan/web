# dependencies
node_modules
.pnp
.pnp.js

# next.js
.next
out
build
next-env.d.ts

# production
dist

# misc
.DS_Store
*.pem
.idea
.vscode

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env
.env
.env.local
.env*.local

# vercel
.vercel

# typescript
*.tsbuildinfo
.env*

# Авто-собираемая лента справочника (генерится при build из git)
src/content/guide/_changelog.generated.json

# iCloud плодит рядом с папками дубликаты вида «X 2» (fileproviderd при
# конфликте синхронизации). 2026-08-24 пустая «arqa 2» попала под `git add -A`,
# git счёл это переименованием — все 76 фотографий застройщика уехали из arqa/
# в «arqa 2». Поймал гейт тестов; чтобы не ловить впредь — не пускаем в индекс.
* 2
* 2.*
