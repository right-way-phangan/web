# Verify log — knowledge-guide accuracy checks

Ledger for the monthly re-verification pass (`verify-guides.yml` → `verify-routine.md`).
One row per guide, updated in place each time it is re-checked. A slug NOT listed here has
never been verified since the guide was authored.

`verdict`: `ok` (still accurate) · `updated` (content corrected, `updated` bumped) ·
`needs-human` (couldn't confirm — Vladimir to check).

| slug | kbId | faqCategory | last-verified | verdict | note |
|------|------|-------------|---------------|---------|------|
